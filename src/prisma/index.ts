/**
 * Barrel exports for the Prisma module.
 *
 * Allows other modules to import from a single path:
 *   import { PrismaModule, PrismaService } from '../prisma/index.js';
 * instead of importing from individual files.
 */
export { PrismaModule } from './prisma.module.js';
export { PrismaService } from './prisma.service.js';
